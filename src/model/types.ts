/**
 * Various generic interfaces and typedefs.
 */

/**
 * An ID from the server -- we don't care whether it's a number
 * or string.
 */
export type ServerID = string | number;

export type Url = string;

export type Money = string;

/**
 * A voltage level.
 */
export type Level = string;
